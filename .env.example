# AI CLI Environment Configuration Example

# API Token for authenticated access (get better rate limits)
API_TOKEN=your_token_here

# Optional: Choose AI models (leave as defaults if using standard models)
# TEXT_MODEL=openai          # Options: openai, mistral, gemini, etc. (check 'models' command for current list)
# IMAGE_MODEL=flux           # Options: flux, kontext, turbo, etc. (check 'models' command for current list)

# Optional: Custom API endpoints (leave as defaults if using standard service)
# TEXT_API_URL=https://text.pollinations.ai
# IMAGE_API_URL=https://image.pollinations.ai/prompt
